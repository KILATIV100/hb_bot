# 🤖 XBrovary Feedback Bot

Телеграм-бот для збору зворотного зв'язку, новин та реклами для каналу XBrovary.

## 🌟 Основний функціонал

### 1. Для користувачів
* **Інтерактивне меню:** Зручні кнопки "📰 Надіслати новину", "📢 Запит про рекламу", "💬 Інше".
* **Пряма відправка:** Можна просто написати в бот або скинути фото/відео — воно автоматично потрапить у категорію "Інше".
* **Підтримка медіа:** Текст, фото (альбоми), відео, документи.
* **Антиспам:** Обмеження — 1 повідомлення на хвилину.
* **Зворотний зв'язок:** Користувач отримує сповіщення, якщо адмін відповів на його повідомлення.

### 2. Для адміністраторів
* **Сповіщення:** Миттєві повідомлення в особисті про нові заявки.
* **Керування:** Кнопки під кожною заявкою:
    * `💬 Відповісти` — надіслати текст користувачеві від імені бота.
    * `✅ Опублікувати` — відправити пост на основний канал.
* **Водяні знаки (Watermarks):**
    * Автоматичне накладання логотипу **XBrovary**.
    * **Підтримка:** Фото та Відео.
    * **Розміщення:** 5 логотипів (4 кути + центр).
    * **Стиль:** Напівпрозорі (30%), з відступом від країв.
* **Аналітика:** Команда `/stats` (день/тиждень/за весь час).
* **Фільтри:** Перегляд останніх заявок (`/news`, `/ads`, `/other`).

---

## 🛠 Технічні вимоги

* Python 3.10+
* PostgreSQL
* FFmpeg (для обробки відео)

### Залежності
Всі бібліотеки вказані у `requirements.txt`:
* `aiogram` (бот)
* `psycopg` (база даних)
* `Pillow` (обробка фото)
* `moviepy` (обробка відео)

---

## 🚀 Інструкція з використання (Адмін-панель)

### 1. Отримання заявок
Коли приходить нове повідомлення, адмін бачить:
* Категорію (Новина/Реклама/Інше).
* Юзернейм та ID автора.
* Вміст (текст/медіа).
* Кнопки дій.

### 2. Публікація та Водяні знаки
При натисканні `✅ Опублікувати`:
1.  Якщо це **текст** — публікується миттєво.
2.  Якщо це **фото або відео**, бот запитає:
    * `✅ З вотермаркою`
    * `❌ Без вотермарки`
3.  При виборі "З вотермаркою":
    * **Фото:** Обробляється миттєво.
    * **Відео:** Запускається процес рендерингу (займає 10-40 секунд). Адмін отримає повідомлення про початок і кінець обробки.
    * Логотипи накладаються у 5 точках кадру.

### 3. Команди
Писати прямо в бот (доступно тільки адмінам):
* `/start` — перезапуск, головне меню.
* `/stats` — статистика повідомлень.
* `/news` — показати останні 20 новин.
* `/ads` — показати останні 20 запитів реклами.
* `/other` — показати останні 20 інших повідомлень.
* `/id` — дізнатися свій Telegram ID.

---

## ⚙️ Встановлення та Налаштування

1.  **Клонування репозиторію:**
    ```bash
    git clone <url>
    cd hb_bot
    ```

2.  **Змінні оточення (.env):**
    Створіть файл `.env` і заповніть:
    ```ini
    BOT_TOKEN=ващ_токен
    ADMIN_IDS=12345678,87654321
    CHANNEL_ID=@ващ_канал
    DATABASE_URL=postgresql://user:pass@host:port/dbname
    ```

3.  **Логотип:**
    Завантажте файл логотипу у форматі PNG за шляхом:
    `assets/xbrovary_logo.png`

4.  **Запуск:**
    ```bash
    pip install -r requirements.txt
    python main.py
    ```
