# 🚀 Налаштування бота на Railway (або іншому сервері)

## 📋 Крок 1: Встановіть змінні середовища (Environment Variables)

На вашому сервері (Railway/Render/Heroku) додайте такі змінні:

### Обов'язкові змінні:

```bash
BOT_TOKEN=8522082531:AAH5Z6xLpKh12WNekcq7cr64nhf5Gmw1ts8
CHANNEL_ID=-1001177493799
ADMIN_IDS=[7363233852,768893258,1633275324]
DATABASE_URL=postgresql://postgres:AmeMxvAwDQVfYSQLsZYOiecWraSIfBdG@postgres.railway.internal:5432/railway
```

### ⚠️ ВАЖЛИВО:

- **НЕ ПОТРІБЕН** файл `.env` - бот автоматично читає змінні з сервера
- Формат `ADMIN_IDS` - масив чисел у квадратних дужках
- `CHANNEL_ID` - це ID каналу з мінусом `-1001177493799`
- Після додавання змінних **ОБОВ'ЯЗКОВО** перезапустіть сервіс!

---

## 🔧 Крок 2: Налаштування на Railway

1. Відкрийте ваш проект на Railway
2. Перейдіть у вкладку **Variables**
3. Додайте кожну змінну окремо:
   - Натисніть **New Variable**
   - Введіть назву (наприклад, `BOT_TOKEN`)
   - Введіть значення
   - Натисніть **Add**
4. Після додавання всіх 4 змінних:
   - Натисніть **Deploy** або
   - Перейдіть у **Settings** → **Restart Service**

---

## ✅ Крок 3: Перевірка

Після перезапуску перевірте логи сервісу:

```bash
# У логах повинно бути:
🚀 Бот запущено з підтримкою альбомів!
```

### Швидкий тест (опціонально):

Якщо хочете протестувати локально перед деплоєм:

```bash
# Встановіть залежності
pip install -r requirements.txt

# Запустіть тест
python3 test_bot.py

# Або запустіть повний тест конфігурації
python3 test_config.py
```

---

## 🐛 Troubleshooting (Якщо не працює)

### 1. Бот не відповідає:
- ✅ Перевірте, чи правильно вказаний `BOT_TOKEN`
- ✅ Переконайтеся, що бот не запущений в іншому місці
- ✅ Перевірте логи на помилки

### 2. Не публікує на канал:
- ✅ Перевірте `CHANNEL_ID` (має бути з мінусом!)
- ✅ Додайте бота в канал як адміністратора
- ✅ Дайте боту права на публікацію

### 3. Не отримуєте повідомлення як адмін:
- ✅ Перевірте, чи ваш ID є в `ADMIN_IDS`
- ✅ Напишіть боту `/start` щоб він міг вам писати

### 4. Помилки з базою даних:
- ✅ Перевірте `DATABASE_URL`
- ✅ Переконайтеся, що PostgreSQL база створена
- ✅ Перевірте, чи бот має доступ до бази

---

## 📞 Контакти

Якщо виникли проблеми:
1. Перевірте логи на сервері
2. Запустіть `test_bot.py` для діагностики
3. Переконайтеся, що всі 4 змінні встановлені

---

**Версія:** 1.0
**Дата:** 2025-12-13
